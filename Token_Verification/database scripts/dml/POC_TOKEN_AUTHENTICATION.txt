ALTER TABLE TOKEN_AUTHENTICATION 
RENAME TO POC_TOKEN_AUTHENTICATION;

ALTER TABLE POC_TOKEN_AUTHENTICATION  
MODIFY (TOKEN VARCHAR2(250 CHAR) );

ALTER TABLE POC_TOKEN_AUTHENTICATION  
MODIFY (PAYLOAD VARCHAR2(250 CHAR) );

ALTER TABLE POC_TOKEN_AUTHENTICATION  
MODIFY (TIME_STAMP DEFAULT systimestamp );

ALTER TABLE POC_TOKEN_AUTHENTICATION  
MODIFY (USER_ID VARCHAR2(250 CHAR) );

ALTER TABLE POC_TOKEN_AUTHENTICATION 
ADD (LIFE_TIME NUMBER );
