CREATE TABLE TOKEN_AUTHENTICATION 
(
  TOKEN VARCHAR2(250) NOT NULL 
, PAYLOAD VARCHAR2(250) 
, TIME_STAMP TIMESTAMP 
, COUNT NUMBER 
, USER_ID VARCHAR2(250) 
, CONSTRAINT TOKEN_AUTHENTICATION_PK PRIMARY KEY 
  (
    TOKEN 
  )
  ENABLE 
);
